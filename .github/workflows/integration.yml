name: Integration

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  merge_group:
    types: [checks_requested]
  workflow_dispatch:

permissions: read-all

env:
  CARGO_TERM_COLOR: always

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - name: Check out repo
        uses: actions/checkout@v6

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy

      - name: Cache dependencies
        uses: Swatinem/rust-cache@v2

      - name: Run linters
        run: ./bin/lint

  alpine-linux:
    name: Run on Alpine Linux
    needs: lint
    runs-on: ubuntu-latest
    container:
      image: rust:alpine
    steps:
      - name: Check out repo
        uses: actions/checkout@v6

      - name: Install dependencies
        run: |
          apk add --no-cache build-base bash

      - name: Build rv
        run: ./bin/build

      - name: Test rv runs
        run: |
          ./target/release/rv --version
          ./target/release/rv --help

  arch-linux:
    name: Run on Arch Linux
    needs: lint
    runs-on: ubuntu-latest
    container:
      image: archlinux:base-devel
    steps:
      - name: Check out repo
        uses: actions/checkout@v6

      - name: Install dependencies
        run: pacman -Syu --noconfirm rust

      - name: Build rv
        run: ./bin/build

      - name: Test rv installs and runs ruby
        run: |
          ./target/release/rv --version
          ./target/release/rv --help
          ./target/release/rv ruby run 3.4.7 -- -e 'puts RUBY_DESCRIPTION'

  macos:
    name: Run on macOS
    needs: lint
    runs-on: macos-latest
    steps:
      - name: Check out repo
        uses: actions/checkout@v6

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Cache dependencies
        uses: Swatinem/rust-cache@v2

      - name: Build rv
        run: ./bin/build

      - name: Test rv installs and runs ruby
        run: |
          ./target/release/rv --version
          ./target/release/rv --help
          ./target/release/rv ruby run 3.4.7 -- -e 'puts RUBY_DESCRIPTION'

  wsl-windows:
    name: Run on WSL (Windows)
    needs: lint
    runs-on: windows-latest
    steps:
      - name: Check out repo
        uses: actions/checkout@v6

      - name: Install WSL Ubuntu
        run: wsl --install -d Ubuntu-24.04 --no-launch

      - name: Install dependencies
        run: wsl -d Ubuntu-24.04 -u root -- bash -c "apt-get update && apt-get install -y build-essential curl"

      - name: Install Rust
        run: wsl -d Ubuntu-24.04 -- bash -c "curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y"

      - name: Build rv
        run: wsl -d Ubuntu-24.04 -- bash -c "source ~/.cargo/env && cargo build --release --locked --bin rv"

      - name: Test rv installs and runs ruby
        run: |
          wsl -d Ubuntu-24.04 -- ./target/release/rv --version
          wsl -d Ubuntu-24.04 -- ./target/release/rv --help
          wsl -d Ubuntu-24.04 -- ./target/release/rv ruby run 3.4.7 -- -e 'puts RUBY_DESCRIPTION'

  native-windows:
    name: Run on Native Windows
    needs: lint
    runs-on: windows-latest
    steps:
      - name: Check out repo
        uses: actions/checkout@v6

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Cache dependencies
        uses: Swatinem/rust-cache@v2

      - name: Build rv
        run: cargo build --release --locked --bin rv

      - name: Run rv ruby run (install Ruby 3.4.4)
        run: .\bin\integration-tests\windows\ruby-run.ps1 .\target\release\rv.exe

      - name: Run rv ruby list
        run: .\bin\integration-tests\windows\ruby-list.ps1 .\target\release\rv.exe

      - name: Run rv ruby find
        run: .\bin\integration-tests\windows\ruby-find.ps1 .\target\release\rv.exe

      - name: Run rv ruby pin
        run: .\bin\integration-tests\windows\ruby-pin.ps1 .\target\release\rv.exe

      - name: Run rv shell env
        run: .\bin\integration-tests\windows\shell-env.ps1 .\target\release\rv.exe

      - name: Run rv cache dir
        run: .\bin\integration-tests\windows\cache-dir.ps1 .\target\release\rv.exe

      - name: Run rv ruby uninstall
        run: .\bin\integration-tests\windows\ruby-uninstall.ps1 .\target\release\rv.exe

      - name: Run rv ci
        run: .\bin\integration-tests\windows\ci.ps1 .\target\release\rv.exe
