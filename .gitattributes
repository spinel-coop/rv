# Ensure consistent line endings across all platforms.
# Windows CI runners default to core.autocrlf=true, which converts LF to CRLF
# on checkout. This breaks fixture-based tests where Rust code generates LF but
# fixture files are read with CRLF.
* text=auto eol=lf
