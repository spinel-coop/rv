# frozen_string_literal: true

RSpec.describe {{ module_decl }} do
  it "has a version number" do
    expect({{ module_decl }}::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
