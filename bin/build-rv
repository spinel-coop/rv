#!/bin/sh
set -eu

# Build rv in release mode with all features
# Used by Makefile, smoke tests, and CI

DIR="$(cd "$(dirname "$0")/.." && pwd)"

cd "$DIR"
exec cargo build --release --locked --all-features --bin rv
