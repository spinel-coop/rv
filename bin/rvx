#!/bin/bash
set -euo pipefail
DIR="$( cd "$( dirname "${BASH_SOURCE[0]}"/ )/.." && pwd )"

echo "+ cargo build --release -q --bin rvx"
(
    cd "$DIR"
    cargo build --release -q --bin rvx
)

set -x
exec "$DIR"/target/release/rvx "$@"
